# Environment variables for the Translation Pipeline Application

# Port for the server (default: 3000)
PORT=3000

# Gemini API key for PDF processing with Marker
GEMINI_API_KEY=your_gemini_api_key_here

# Directory paths (optional - uses defaults if not set)
DATA_DIR=./data
UPLOADS_DIR=./uploads
MARKER_OUTPUT_DIR=./data/marker_output